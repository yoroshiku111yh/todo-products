import { styled } from "@mui/material";


export const ContainerWrapper = styled("div")({
    position : "relative",
    maxWidth : "523px",
    margin: "30px auto",
    padding : "30px 15px",
    background : "#fff",
    borderRadius : '15px',
    boxShadow: "1px 1px 12px -1px rgba(156,152,152,0.75)"
});